# 调优步骤
- 发现问题
- 定位问题
- 解决问题

## JVM 调优步骤
- 监控发现问题
- 工具分析问题
- 性能调整 （调优）

Prometheus + Grafana
## 监控什么问题
- GC频繁
- CPU 负载过高
- OOM （out of memory)
- 内存泄露
- 死锁
- 响应时间长



## jvm吞吐量和停顿时间
JVM吞吐量和停顿时间是指两个不同的性能指标，但它们都直接影响着Java应用程序的性能表现。

### 吞吐量（Throughput）：
指系统在特定时间内处理请求的数量。对于Java应用程序，吞吐量可以用每秒编译的字节码量来衡量，或者是处理的业务数据量。

### 停顿时间（Pause Time）：
指停止应用程序以进行垃圾收集或其他维护任务的时间。停顿时间通常包括两部分：垃圾收集的停顿时间（GC pause time）和其他原因导致的应用程序停顿时间。

为了提升吞吐量和降低停顿时间，可以采取以下措施：

垃圾收集策略：选择合适的垃圾收集器，调整堆大小和垃圾收集相关参数，以减少GC的频率和持续时间。

代码优化：优化Java代码，减少不必要的对象创建，使用缓存和非阻塞算法以提高处理速度。

JIT编译优化：通过JIT编译器的优化，提高执行本地代码的效率。

应用架构优化：分布式处理，负载均衡，异步通信等方式提高系统整体吞吐量。

硬件资源：提升硬件性能，如使用更快的CPU，增加内存，提高网络带宽等。

具体到JVM参数调优，可以通过设置如下参数来影响吞吐量和停顿时间：

-Xmx 和 -Xms：设置堆的最大和最小值。

-XX:NewRatio 和 -XX:SurvivorRatio：调整新生代和幸存者区的大小。

-XX:+UseG1GC：选择G1垃圾收集器。

-XX:MaxGCPauseMillis：设置期望的GC最大暂停时间。

-XX:ParallelGCThreads：设置并行收集器的线程数。

-XX:ConcGCThreads：设置并发收集器的线程数。

具体调优应考虑实际应用场景和需求，进行实际的测试和对比。

## JVM 调优依据

JVM 调优时，吞吐量和停顿时间无法兼顾，吞吐量提高的代价是停顿时间的拉长。
所以，应用程序与用户不交互，就优先提升吞吐量。如果交互频繁，就优先缩短交互时间。

## JDK 自带命令行调优工具
- jps 查看正在运行的java进程
- jstat 查看指定进程的JVM统计信息
- jinfo 实时查看和修改指定进程的JVM配置参数
- jstack 打印指定进程此刻的线程快照。 定位线程长时间停顿的原因，如死锁，等待资源，阻塞。

## jvm 新生代 老年代
JVM中的堆内存被划分为新生代和老年代。新生代主要用于存放新创建的对象，而老年代存放新生代中经过多次垃圾回收仍然存活的对象。

- 新生代 (Young Generation)

新生代进一步划分为 Eden 区和两个大小相等的 Survivor 区（通常称为 From 和 To）。当新对象被创建时，它们首先被放入 Eden 区。当 Eden 区满时，执行 Minor GC。在这个过程中，还存活的对象会被移动到 Survivor 区，并在那里对象会在一些集合中来回移动，直到被认为是老年的，然后被晋升到老年代。

- 老年代 (Old Generation)

老年代存放新生代中经过多次垃圾回收仍然存活的对象。当老年代空间不足时，会触发Major GC/Full GC。

调整JVM新生代和老年代的大小可以通过JVM启动参数 -Xms（堆的起始大小）和 -Xmx（堆的最大大小）来设置，新生代和老年代的大小比例通常是1:2（即新生代为整个堆的1/3，老年代为整个堆的2/3），但这也可以通过-XX:NewRatio参数来调整。

例如，以下是设置JVM堆大小和新生代与老年代比例的示例：

java -Xms512m -Xmx1024m -XX:NewRatio=2 MyApplication
在这个例子中，JVM堆被设置为起始大小为512MB，最大大小为1024MB，新生代占整个堆的1/3，老年代占2/3。


## jstat 查看内存溢出
jstat 是一个用于监视 Java 虚拟机(JVM)中各种不同的状态信息的命令行工具。它可以用于查看堆内存的使用情况，类加载信息，垃圾收集信息等。

如果你需要使用 jstat 来检查是否有内存溢出，你可以定期使用以下命令来监视 Java 应用程序的内存使用情况：

jstat -gc <pid> <interval>
其中 <pid> 是你想要监视的 Java 进程的ID，<interval> 是收集统计信息的间隔时间（以毫秒为单位）。

例如，要每隔一秒收集一次进程ID为1234的Java应用程序的垃圾收集信息，你可以运行：

jstat -gc 1234 1000
在输出中，你可以注意到以下几列的变化：

S0C、S1C、S0U、S1U：这些代表Survivor区的容量和使用情况。

EC、EU：这些代表Eden区的容量和使用情况。

OC、OU：这些代表老年代的容量和使用情况。

MC、MU：这些代表元数据区（元空间）的容量和使用情况。

CCSC、CCSU：这些代表压缩类空间的容量和使用情况。

YGC、YGT：这些代表年轻代垃圾收集的次数和总耗时。

FGC、FGCT：这些代表全局垃圾收集的次数和总耗时。

如果你看到 OU（老年代使用量）一直增加而不降低， FGC 的次数在增加，这可能表明应用程序正在经历频繁的全局垃圾收集，即老年代内存溢出的迹象。

注意，jstat 不能直接分析出内存溢出，但可以帮助识别潜在的内存问题。 若要进一步诊断，你可能需要使用其他工具，如 jmap 或 jhat，或者启用详细的 JVM 日志记录。

## JDK 自带饿可视化监控工具
- jconsole
- Visual VM: